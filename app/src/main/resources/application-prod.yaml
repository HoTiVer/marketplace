spring:
  flyway:
    locations: classpath:db/migration/common

logging:
  level:
    root: info
    org.hotiver: info
    org.springframework.security: warn
    org.hibernate.SQL: warn

springdoc:
  swagger-ui:
    enabled: false
  api-docs:
    enabled: false