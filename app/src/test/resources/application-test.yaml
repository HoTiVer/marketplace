spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  data:
    redis:
      host: localhost
      port: 6379
  flyway:
    enabled: true

jwt:
  secret: test-secret
  refresh:
    expiration: 3600000
  access:
    expiration: 600000
